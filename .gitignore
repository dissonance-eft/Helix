# ── Python ──
__pycache__/
*.pyc
*.pyo
*.pyd
*.egg-info/

# ── Virtual environments ──
.venv/
venv/
env/

# ── OS ──
.DS_Store
Thumbs.db

# ── Editors ──
.vscode/
.idea/

# ── Jupyter ──
.ipynb_checkpoints/

# ── Temp / logs ──
*.log
*.tmp
tmp_*.py

# ── AI tool config (not for version control) ──
.claude/
.agents/
.agent/
_agents/
_agent/

# ── Snapshots ──
*.zip
helix_snapshot.zip

# ── Artifacts directory (agent-generated, not source) ──
artifacts/

# ── Library: raw data / priors (large, not version-controlled) ──
codex/library/

# ── Binary / rendered outputs ──
*.nrg
*.bin
*.iso

# ── Lab outputs ──
applications/labs/outputs/
